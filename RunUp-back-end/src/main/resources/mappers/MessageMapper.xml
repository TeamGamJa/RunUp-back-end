<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="message">

	<insert id="insert" parameterType="Message">
		INSERT INTO message (MessageNum, MessageSender, MessageReceiver, MessageTitle,
		MessageContent, MessageDate, UserNum)
		VALUES (#{messageNum}, #{messageSender}, #{messageReceiver}, #{messageTitle}, #{messageContent}, Now(), #{userNum})
	</insert>


</mapper>