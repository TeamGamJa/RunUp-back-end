<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="report">
   
   <insert id="createReport" parameterType="Report">
     insert into report(reporter, accused, reportContent, reportResult, reportCategory, reportFile, userNum) 
     values (#{reporter},#{accused},#{reportContent},#{reportResult},#{reportCategory},#{reportFile},#{userNum})
   </insert>
   
</mapper>